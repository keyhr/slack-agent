use_default_settings:
  engines:
    remove:
      - wikidata
      - ahmia
      - torch

server:
  # SEARXNG_SECRET 環境変数で上書きされる（docker-entrypoint.sh が ultrasecretkey を置換）
  secret_key: "ultrasecretkey"
  limiter: false

outgoing:
  request_timeout: 20.0

search:
  formats:
    - html
    - json
